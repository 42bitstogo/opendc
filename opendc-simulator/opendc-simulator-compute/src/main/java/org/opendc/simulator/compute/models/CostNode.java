package org.opendc.simulator.compute.models;

import org.opendc.simulator.engine.FlowNode;

public class CostNode extends FlowNode {

    //adit-TODO: implement oUpdate

    /*fun registerHost(host: SimHost) {
        hostMap[host.uid] = host
        // Initialize cost for the new host
        val initialCost = costTrace.getCost(host.uid, startTime) ?: 0.0
        host.updateCost(initialCost)
        // Schedule the first cost update
        val nextUpdate = costTrace.getNextCostUpdate(host.uid, startTime)
        nextCostUpdates[host.uid] = nextUpdate
    }*/

}
